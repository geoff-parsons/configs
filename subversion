
[miscellany]

  # Set global-ignores to a set of whitespace-delimited globs
  # which Subversion will ignore in its 'status' output, and
  # while importing or adding files and directories.
  global-ignores = *.o .DS_Store

